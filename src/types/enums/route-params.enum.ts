export enum RouteParamTypes {
    REQ = 'req',
    RES = 'res',
    NEXT = 'next',
}

export enum RouteRequestParamTypes {
    HEADER = 'headers',
    PARAM = 'params',
    QUERY = 'query',
    BODY = 'body',
    LOGGER = 'logger',
}
